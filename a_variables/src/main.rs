fn main() {
    const NUMBER_OF_MISSILES = 9;
    const NUMBER_OF_READY = 2;
    let (missiles, ready) = (NUMBER_OF_MISSILES, NUMBER_OF_READY);
    println!("Firing {} out of {}", ready, missiles);
}
